{%- capture url -%}
    {% for pt in site.post_tags %}
        {% if pt.title == include.tag %}
            {{ pt.url }}
        {% endif %}
    {% endfor %}
{%- endcapture -%}
{{ url | strip | default: "/blog/tags/" | relative_url}}
